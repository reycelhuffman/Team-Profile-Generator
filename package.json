{
  "name": "lib",
  "version": "1.0.0",
  "description": "Your task is to build a Node.js command-line application that takes in information about employees on a software engineering team, then generates an HTML webpage that displays summaries for each person. Testing is key to making code maintainable, so you’ll also write a unit test for every part of your code and ensure that it passes each test.",
  "main": "Employee.js",
  "scripts": {
    "test": "jest --verbose"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "init": "^0.1.2",
    "inquirer": "^8.2.3"
  },
  "devDependencies": {
    "jest": "^27.5.1"
  },
  "directories": {
    "lib": "lib"
  }
}
